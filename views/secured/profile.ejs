<!DOCTYPE html>
<html>
<head>
	<title>Node Authentication</title>
	   <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
    <style>
        body        { padding-top:80px; word-wrap:break-word; }
    </style>
</head>
<body>
<div class="container">
	<div class="page-header text-center">
		<h1><span class="fa fa-anchor"></span>PROFILE PAGE</h1>
		<a href="/auth/logout" class="btn btn-default btn-sm pull-right">LOGOUT</a>
	</div><!--END OF THE PAGE HEADER-->
	<div class="row">
	<!--FACEBOOK INFORMATION-->
	<div class="col-sm-6">
	<div class="well">
		<h3 class="text-primary"><span class="fa fa-facebook"></span></h3>
		<!--Check if the user has the FB token-->
		<% if(user.facebook.token) {%>
		<p>
			<strong>id</strong><%= user.facebook.id%><br>
			<strong>token</strong><%= user.facebook.token%><br>
			<strong>email</strong><%= user.facebook.email%><br>
			<strong>name</strong><%= user.facebook.name%><br>
		</p>	
		<a href="/auth/unlink/facebook" class="btn btn-primary">UNLINK</a>
		<%} else {%>
		<a href="/auth/connect/facebook">Connect Facebook</a>
		<%} %>
	</div><!--END OF WELL-->
	</div><!--END OF col-sm-6-->

	<!--GOOGLE INFORMATION-->
	<div class="col-sm-6">
		<div class="well">
			<h3 class="text-danger"><span class="fa fa-google-plus"></span>GOOGLE PLUS</h3>

		<!--CHECK IF THE USER HAS GOOGLE TOKEN-->
		<% if (user.google.token) { %>
        <p>
        <strong>id</strong>: <%= user.google.id %><br>
        <strong>token</strong>: <%= user.google.token %><br>
        <strong>email</strong>: <%= user.google.email %><br>
        <strong>name</strong>: <%= user.google.name %>
        </p>
        <a href="/auth/unlink/google" class="btn btn-danger">Unlink</a>
        <% } else { %>
        <a href="/auth/connect/google" class="btn btn-danger">Connect Google</a>
        <% } %>
		</div><!--END of well-->
	</div><!--END OF col-sm-6-->
</div>

<div class="row">
<!--TOKEN AUTHENTICATION BLOCK-->
	<div class="col-sm-6">
		<div class="well">
		<h3 class="text-warning"><span class="fa fa-cogs"></span>API TOKEN</h3>
		<p>
			<% if(user.token){ %>
				<strong>Token</strong> : <%= user.token.value %><br>
			<% } else { %>
				<a href="/getToken" class="btn btn-warning">GET TOKEN</a>
			<%}%>
	</div>
	</div>
</div><!---END OF THE ROW-->
</div><!--END OF THE CONTAINER-->
</body>
</html>